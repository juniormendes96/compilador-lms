PROGRAM fibonacci;

    VAR x, y, soma: INTEGER;

    BEGIN

        x := 0;
        y := 1;

        FOR i := 0 TO i < 30 DO
            soma := x + y;
            x := y;
            y := sum;
            WRITELN(soma);
END.